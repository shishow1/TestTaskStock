@model IEnumerable<TestTaskStock.Models.Stockman>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FullName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FullName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StockmanID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StockmanID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StockmanID })
        </td>
    </tr>
}

</table>
